
# push changes to github
git push

# pull updates from github
git pull origin master

# or you can do the following commands and after that you can just do 'git pull'
git config branch.master.remote origin
git config branch.master.merge master

# set colors on for all your git repos
git config --global color.ui auto

# deal with line endings
# this will prevent you from accidentally checking in code with crlf line endings
git config --global core.autocrlf input

# other useful stuff
git status
git diff
git diff --cached
git commit -a
git add
git rm
git checkout


